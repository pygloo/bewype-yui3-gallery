    /**
     *
     */

    var Bewype = function(config) {
        Bewype.superclass.constructor.apply(this, arguments);
    };

    /**
     *
     */
    Bewype.NAME = "bewype";

    /**
     * @class Bewype
     */
    Y.extend( Bewype, Y.Base, {

        initializer: function( config ) {
        },

        destructor: function() {
        }

    } );

    Y.Bewype = Bewype;

